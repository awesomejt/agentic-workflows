#!/usr/bin/env python3
"""Run workflowctl directly from a repository checkout."""

from pathlib import Path
import sys

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "tools" / "workflowctl" / "src"))

from workflowctl.cli import main  # noqa: E402

raise SystemExit(main())
