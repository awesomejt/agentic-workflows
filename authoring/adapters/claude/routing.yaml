schema_version: 1
id: claude
common_role_catalog: authoring/common/roles/catalog.yaml
defaults:
  model: inherit-active-model
  permission_strategy: native-role-frontmatter
overrides:
  - role: orchestrator
    permission_profile: delegate-and-write-loop-state
    reason: The loop coordinator must invoke specialists and maintain transient state.
