schema_version: 1
id: codex
common_role_catalog: authoring/common/roles/catalog.yaml
defaults:
  model: inherit-active-model
  permission_strategy: native-agent-toml-sandbox
overrides:
  - role: orchestrator
    permission_profile: workspace-write-loop-state
    reason: The coordinator maintains handoffs while the primary Codex session delegates agents.
